name: Rust - Run tests
description: "Run tests for crate"

inputs:
    release:
        description: "Work on release version"
        default: false

runs:
    using: "composite"
    steps:
        - run: cargo test ${{ inputs.release == 'true' && '--release' || '' }}
          shell: bash
